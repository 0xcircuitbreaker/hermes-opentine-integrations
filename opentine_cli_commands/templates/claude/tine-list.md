# Claude Code slash commands for opentine
# Place these in ~/.claude/commands/ for Claude Code to pick them up.

List all opentine agent runs from the runs directory (default: ~/.tine_runs/).
Use the opentine Python API or CLI to list all .tine files, showing run ID,
step count, status, cost, and duration in a table.

Arguments: $ARGUMENTS
