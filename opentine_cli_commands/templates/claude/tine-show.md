Show full details of opentine run $ARGUMENTS.
Load the .tine artifact and display every step: kind, ID, parent, inputs,
outputs, errors, tool info, cost, and duration. Use the opentine Python API
(Run.load) to read the artifact from the runs directory (default ~/.tine_runs/).
